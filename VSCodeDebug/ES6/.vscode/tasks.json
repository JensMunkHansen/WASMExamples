{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "StartServer",
            "type": "shell",
            "command": "npx serve -p 3000 .",
            "problemMatcher": [],
            "isBackground": true,
            "options": {
                "cwd": "${workspaceFolder}/build/Debug" // Set the working directory
            }            
        }
    ]
}
