{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Emscripten Hello",
            "type": "node",
            "request": "launch",
            "program": "build/Debug/${command:cmake.launchTargetFilename}",
        }
    ]
}
